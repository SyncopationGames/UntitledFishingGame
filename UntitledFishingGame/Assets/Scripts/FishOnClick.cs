using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FishOnClick : MonoBehaviour
{
    Vector2 mouseStart;
    Vector2 mouseEnd;

    public bool click;
    bool controllerClick;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
